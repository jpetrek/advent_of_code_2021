#pragma once
#include "helper.h"

class day20 : public day_base<20>
{
    void run_interal() override
    {
        /*
        while (!input_reader().is_end_of_file())
        {
        }
        */
    }
};
